

fn run_client () {
    println!("Welcome to saturn client...");

    println!(">");

    let mut reader = io::stdin();
    let input = reader.read_line().unwrap();
    // parse the command
    // send it to the server
}
